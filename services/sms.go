/*package services

import (
	"net/http"
	"sync"
)

// SMSRequest struct
type SMSRequest struct {
	URL        string
	APIKey     string
	Messages   []string
	Recipients []string
	Status     []bool
}

// SendMessage initiates sending of the messages
func SendMessage(ei int, email *Request, wg *sync.WaitGroup) {
	req, err := http.NewRequest(method, c.baseURL+"/"+uri, payload)

	if err != nil {
		return err
	}
}


*/